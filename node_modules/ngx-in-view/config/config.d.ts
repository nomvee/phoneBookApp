import { EventEmitter, OpaqueToken } from '@angular/core';
import { IInViewConfig } from './config.interface';
export declare const INV_CONFIG: OpaqueToken;
export declare class InViewConfig {
    _emitter: EventEmitter<{}>;
    private values;
    constructor(config?: IInViewConfig);
    update(config: IInViewConfig): void;
    get(key: string): any;
}
export declare function InViewConfigurable(config?: {
    changeDetectorProperty: string;
}): (constructor: Function) => void;
