import { EventEmitter, ChangeDetectorRef, ElementRef } from '@angular/core';
import { InViewConfig } from './config/config';
import { Enter, Exit, EnterOnce, ExitOnce } from './output';
export declare class InView {
    private config;
    private element;
    private cd;
    onEnter: EventEmitter<any>;
    onExit: EventEmitter<any>;
    onEnterOnce: EventEmitter<any>;
    onExitOnce: EventEmitter<any>;
    private uid;
    private hasEnterHandler;
    private hasExitHandler;
    private hasEnterOnceHandler;
    private hasExitOnceHandler;
    constructor(config: InViewConfig, element: ElementRef, cd: ChangeDetectorRef, enter: Enter, exit: Exit, enterOnce: EnterOnce, exitOnce: ExitOnce);
    ngAfterViewInit(): void;
    is(): any;
    private invOnConfigChanges();
}
